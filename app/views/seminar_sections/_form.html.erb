<%= form_for(@seminar_section) do |f| %>
  <% if @seminar_section.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@seminar_section.errors.count, "error") %> prohibited this seminar_section from being saved:</h2>

      <ul>
      <% @seminar_section.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :section %><br>
    <%= f.text_field :section %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
